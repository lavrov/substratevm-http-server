Example build of http server compiled with [native-image](https://www.graalvm.org/docs/reference-manual/aot-compilation/).

Build docker image:
```sh
$ make
```

Run http server inside docker container:
```sh
$ make run-docker
```